extends layout

block content
  h1= title
  h3 Welcome to the queue!
  div
    p Oldest
    ul#queueList(class='list-group')
      each val in queue
        li(class='list-group-item')=val.name
    p Newest
  button#btnEnqueue(class='btn') Enqueue
